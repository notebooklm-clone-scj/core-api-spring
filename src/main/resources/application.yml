spring:
  # 1. PostgreSQL ?????? ?? ?? (?? ??? ?? ?? ???!)
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/notebooklm_db
    username: chanjin
    password: chanjinpassword

  # 2. JPA (?????? ORM) ??
  jpa:
    hibernate:
      ddl-auto: update # ?? ??? ?? DB ???? ??? ????? ??? ??
    show-sql: true # ???? ???(SELECT, INSERT ?)? ??? ???
    properties:
      hibernate:
        format_sql: true

  # 3. Redis ?? ?? ?? ??
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  # 이 키는 외부로 유출되면 안 됩니다! (최소 32바이트 이상 길이의 임의 문자열)
  secret: notebooklm-clone-project-chanjin-secret-key-very-long-string
  access-expiration: 1200000 # 20분
  refresh-expiration: 604800000 # 7일


ai-worker:
  url: http://localhost:8000
  connect-timeout-ms: 3000
  read-timeout-ms: 180000
  chat-retry-count: 1
